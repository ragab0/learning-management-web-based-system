import React from "react";

export default function SaveCourse() {
  function saveHandler() {}

  return (
    <button claassName="btn btn-primary" onClick={saveHandler}>
      Save Changes
    </button>
  );
}
